# commonplace-value

Created 2026-08-25 at jes's request. Purpose not yet specified by him.

## Working arrangement (jes, 2026-08-25T03:32Z)

- **Toplevel model: Opus 5.** Launched as `workerclaude --model claude-opus-5`.
- **Implementers: Sol, in tmux panes** — ruling #25 stands for this repo. Opus subagents are the
  fallback only when Sol is unavailable.

The Sol dispatch recipe is not reproduced here on purpose; ask `commonplace-doc` or
`commonplace-dir` on clod-squad, which have run it all day. Their rounds start deliberately RED and
land through a script that can refuse.

## Sibling context

Part of the commonplace family: `commonplace-log` and `commonplace-log-reducer` at the bottom,
`commonplace-merkle-crdt` and `yepochs` for CRDT and epoch namespaces, `commonplace-doc` for
Documents, `commonplace-dir` for Directories, `commonplace-doc-sync` for movement between
endpoints, `commonplace-plan` for architecture, `commonplace` for the monolith.

`boss-clod/REPO-BOUNDARIES.md` holds the standing rulings; hand it to anyone starting here.
